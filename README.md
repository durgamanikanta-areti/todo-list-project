# todo-list-project
simple todo-list project (vite,react,dummyjson,materialui)

After down loading the project zip file navigate to todo-list(cd todo-list) folder and install dependencies(npm install).
Then type 'npm run dev'.
